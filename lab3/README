get_count():
        for i in 0...array_length:
                if op == LT
                        if array[i] < x
                                count++
                if op == LE
                        if array[i] <= x
                                count++
etc...

reverse():
        for i in 0...array_length // 2:
                temp = array[i]
                array[array_length - 1 - i]
